// in react, we try to minimize the code line as much as possible
// react allows u to write js inside html but its a modified version of html called jsx

let age = 12;

const myComponent = () => {
    return age > 10 ? <div> Ryan </div> : <div> Jay </div>;
}